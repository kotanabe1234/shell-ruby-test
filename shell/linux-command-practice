Linuxコマンド実行まとめ1
 pwd：カレントディレクトリを表示
 cd：指定したディレクトリに移動
 ls：ファイルの一覧を表示する
 ls -a: ファイルの一覧の詳細を表示する
 touch: からのファイルを作成、時刻を変更することが可能
 rm：ファイルを削除する
 rm*:すべてのファイルを消す
 rm -R: ディレクトリを中にあるファイルごと削除
 rm -f: 警告メッセージを表示しないで削除
 mv: ファイルの移動 ファイル名の変更
 mv -i: 移動先に同じファイルの名前があれば上書きしても良いか確認を表示させることが可能
 mv -f: 移動先に同じファイルの名前があっても警告なしで上書きが可能
